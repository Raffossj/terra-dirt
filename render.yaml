services:
  - type: web
    name: terra-hub-validator
    runtime: python311
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: DATABASE_URL
        scope: run
        value: ${DATABASE_URL}
    healthCheckPath: /health
    healthCheckInterval: 30
    maxInstances: 1
